{
  "name": "@json-exploder/cli",
  "version": "1.0.0-beta",
  "description": "JSON Exploder CLI tool",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "keywords": [
    "json",
    "exploder"
  ],
  "author": "Jared Henry",
  "license": "SEE LICENSE IN LICENSE",
  "dependencies": {
    "@json-exploder/api": "1.0.0-beta"
  },
  "devDependencies": {
    "typescript": "^3.3.4000"
  },
  "files": [
    "src/**/*",
    "dist/**/*"
  ]
}
